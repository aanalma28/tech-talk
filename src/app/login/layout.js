import '../../../styles/globals.css'

export default function LoginLayout({ children }) {
  return (
    <html lang="en">
        <head>
            <title>Login Page</title>
        </head>
      <body>{children}</body>
    </html>
  )
}

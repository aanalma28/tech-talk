import '../../../styles/globals.css'

export default function RegisterLayout({ children }) {
  return (
    <html lang="en">
        <head>
            <title>Register Page</title>
        </head>
      <body>{children}</body>
    </html>
  )
}
